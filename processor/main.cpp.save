#include <stdio.h>
#include "processor.h"
#include "..\strings\string_funk.h"
#include "..\hamlet\d_hamlet_functions.h"


int main ()
    {
    size_t lines_num = 0;
    line_buf* code = get_code ("code.txt", &lines_num);

    si
    create_binary (code, lines_num);

    return 0;
    }
